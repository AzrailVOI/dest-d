# 🏗️ Архитектура проекта Dest.d CLI Generator

## 📐 Компонентная структура

Проект следует единой компонентной архитектуре, где каждый модуль разбит на минимальные компоненты.

### Принципы архитектуры:

1. **Максимальный размер файла**: 100 строк (кроме `app.d`)
2. **Один компонент = один файл**: Каждый компонент в отдельном файле
3. **Группировка по функциональности**: Связанные компоненты в одной папке
4. **Package файлы**: Каждая папка имеет `package.d` для экспорта
5. **Единая структура**: Все модули следуют одному паттерну

## 📁 Структура проекта

```
source/
├── app.d                          # Главная точка входа (единственный файл > 100 строк)
│
├── cli/                           # CLI интерфейс
│   ├── package.d                  # Экспорт всех CLI модулей
│   ├── console.d                  # Настройка консоли
│   ├── usage.d                    # Справка по использованию
│   └── commands/                  # Команды CLI
│       ├── package.d              # Экспорт команд
│       ├── router.d               # Роутинг команд
│       ├── module_cmd.d           # Команда module
│       └── generate_cmd.d          # Команда generate
│
├── dest/                          # Dest.d Framework (библиотека)
│   ├── package.d                  # Главный экспорт (import dest;)
│   │
│   ├── decorators/                # Декораторы
│   │   ├── package.d              # Экспорт всех декораторов
│   │   ├── http.d                 # HTTP декораторы (Get, Post, etc.)
│   │   ├── module_decorator.d     # Декоратор Module
│   │   ├── controller.d           # Декоратор Controller
│   │   ├── params.d               # Декораторы параметров (Body, Param, etc.)
│   │   ├── middleware.d           # Декораторы middleware
│   │   └── websocket.d            # WebSocket декораторы
│   │
│   ├── metadata/                  # Метаданные и рефлексия
│   │   ├── package.d              # Экспорт метаданных
│   │   ├── structures.d           # Структуры метаданных
│   │   ├── templates.d            # Шаблоны для работы с UDA
│   │   ├── controller.d           # Извлечение метаданных контроллера
│   │   ├── provider.d             # Извлечение метаданных провайдера
│   │   └── module_extractor.d     # Извлечение метаданных модуля
│   │
│   ├── http/                      # HTTP абстракции
│   │   ├── package.d              # Экспорт HTTP модулей
│   │   ├── request.d              # Класс Request
│   │   └── response.d             # Класс Response
│   │
│   ├── router/                    # Роутер
│   │   ├── package.d              # Экспорт роутера
│   │   ├── router_core.d          # Базовый класс Router
│   │   └── controller_registration.d  # Регистрация контроллеров
│   │
│   ├── application/               # Приложение
│   │   ├── package.d              # Экспорт приложения
│   │   ├── config.d               # Конфигурация приложения
│   │   ├── factory.d              # Фабрика приложений
│   │   ├── application.d          # Класс DestApplication
│   │   ├── server.d               # Настройка сервера
│   │   └── routes.d               # Настройка маршрутов
│   │
│   ├── common/                    # Общие утилиты
│   │   ├── package.d              # Экспорт common модулей
│   │   ├── exceptions.d           # HTTP исключения
│   │   ├── http_status.d          # HTTP статус коды
│   │   ├── validation.d           # Валидация данных
│   │   ├── validators/            # Валидаторы
│   │   │   ├── package.d          # Экспорт валидаторов
│   │   │   ├── interface.d        # Интерфейс IValidator
│   │   │   ├── required.d         # RequiredValidator
│   │   │   ├── length.d           # Min/MaxLengthValidator
│   │   │   ├── email.d            # EmailValidator
│   │   │   └── pattern.d          # PatternValidator
│   │   └── utils/                 # Утилиты
│   │       ├── package.d          # Экспорт утилит
│   │       ├── case_converter.d    # Конвертация регистра
│   │       └── string_helpers.d    # Помощники для строк
│   │
│   ├── di_container.d             # DI контейнер
│   └── module_system.d            # Система модулей
│
├── generators/                    # Генераторы кода
│   ├── destd/                     # Генераторы Dest.d модулей
│   │   ├── package.d              # Экспорт генераторов
│   │   ├── module_gen.d           # Генерация модуля
│   │   └── app_gen.d              # Генерация AppModule/main
│   │
│   ├── advanced/                  # Продвинутые генераторы
│   │   ├── package.d              # Экспорт генераторов
│   │   ├── middleware_gen.d       # Middleware, Guard, Interceptor, Filter, Pipe
│   │   ├── types_gen.d            # Class, Interface, Provider, Decorator
│   │   └── websocket_gen.d        # Gateway, Resolver
│   │
│   ├── advanced_generator.d       # Реэкспорт (обратная совместимость)
│   ├── template_type.d            # Типы шаблонов и компонентов
│   ├── directory_manager.d        # Управление директориями
│   ├── module_scanner.d           # Сканирование модулей
│   ├── config_reader.d            # Чтение конфигурации
│   ├── project_generator.d       # Генерация проекта
│   └── project_file_templates.d   # Шаблоны файлов проекта
│
├── templates/                     # Шаблоны генерации
│   ├── base/                      # Базовые шаблоны
│   │   ├── package.d              # Экспорт базовых шаблонов
│   │   ├── controller.d           # Шаблон контроллера
│   │   ├── service.d              # Шаблон сервиса
│   │   └── module_template.d      # Шаблон модуля
│   │
│   ├── advanced/                  # Продвинутые шаблоны
│   │   ├── package.d              # Экспорт продвинутых шаблонов
│   │   ├── middleware.d           # Шаблон middleware
│   │   ├── guard.d                # Шаблон guard
│   │   ├── interceptor.d          # Шаблон interceptor
│   │   ├── filter.d              # Шаблон filter
│   │   └── pipe.d                 # Шаблон pipe
│   │
│   ├── types/                     # Шаблоны типов
│   │   ├── package.d              # Экспорт шаблонов типов
│   │   ├── class_template.d       # Шаблон класса/интерфейса
│   │   ├── dto.d                  # Шаблон DTO
│   │   ├── entity.d               # Шаблон Entity
│   │   └── provider.d             # Шаблон Provider/Decorator
│   │
│   ├── app/                       # Шаблоны приложения
│   │   ├── package.d              # Экспорт шаблонов приложения
│   │   ├── app_module.d           # Шаблон AppModule
│   │   └── main.d                 # Шаблон main.d
│   │
│   └── websocket/                 # WebSocket шаблоны
│       ├── package.d              # Экспорт WebSocket шаблонов
│       ├── gateway.d              # Шаблон Gateway
│       └── resolver.d             # Шаблон Resolver
│
├── utils/                         # Утилиты CLI
│   ├── string_utils.d             # Строковые утилиты
│   └── validator.d                # Валидация входных данных
│
└── examples/                      # Примеры использования
    ├── main.d                     # Пример main.d
    ├── app_module.d               # Пример AppModule
    └── user_example.d             # Пример модуля User
```

## 🔄 Зависимости между слоями

```
app.d
  └─> cli/
      └─> generators/
          └─> templates/

dest/ (независимый фреймворк)
  ├─> decorators/
  ├─> metadata/
  ├─> http/
  ├─> router/
  ├─> application/
  └─> common/
```

## 📏 Правила организации компонентов

### 1. Размер файлов
- ✅ Максимум 100 строк на файл (кроме `app.d`)
- ✅ Если файл > 100 строк → разбить на компоненты

### 2. Структура папок
- ✅ Каждая функциональная группа в своей папке
- ✅ Каждая папка имеет `package.d` для экспорта
- ✅ Компоненты группируются по функциональности

### 3. Именование
- ✅ Файлы: `snake_case.d`
- ✅ Модули: `package.component`
- ✅ Классы: `PascalCase`
- ✅ Функции: `camelCase`

### 4. Package файлы
- ✅ Всегда экспортируют публичные компоненты
- ✅ Используют `public import` для реэкспорта
- ✅ Содержат документацию модуля

## 🎯 Примеры компонентной структуры

### Пример 1: HTTP модуль
```
dest/http/
├── package.d          # public import dest.http.request; public import dest.http.response;
├── request.d          # Класс Request (~80 строк)
└── response.d         # Класс Response (~100 строк)
```

### Пример 2: Валидаторы
```
dest/common/validators/
├── package.d          # Экспорт всех валидаторов
├── interface.d       # IValidator (~15 строк)
├── required.d         # RequiredValidator (~30 строк)
├── length.d           # Min/MaxLengthValidator (~60 строк)
├── email.d            # EmailValidator (~20 строк)
└── pattern.d          # PatternValidator (~30 строк)
```

## ✅ Преимущества компонентной архитектуры

1. **Читаемость**: Легко найти нужный компонент
2. **Масштабируемость**: Легко добавлять новые компоненты
3. **Тестируемость**: Каждый компонент можно тестировать отдельно
4. **Переиспользование**: Компоненты можно использовать независимо
5. **Поддержка**: Легко вносить изменения в конкретный компонент
